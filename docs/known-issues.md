# 已知问题 / 待办

按主人指示记录、**暂时不改**的问题。修完把对应条目删掉，别留着当装饰。

当前没有未处理的条目。

---

## 修复纪律

- UI 布局问题**改完必须真的截一张图核对**，不能只看测试通过 —— 这个项目的 UI 测试
  只走 Python 控件树，看不见真实渲染结果。
  这条不是空话：1.0.6 修「账号/IP 文字重叠」时，第一版修复在测试里是绿的，截图一
  看仍然重叠 —— 真正的原因是文字宽度把中日韩全角字符按半角算了。
- 顺带记住：HUD 画布高度是固定一次的，**申请得比布局给的多就会被裁**，而底部读数
  正好在最下面，最先被裁掉。
